import { StyleSheet } from 'react-native'

export const GameCardStyle = (StyleSheet.createstyle = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    alignItems: 'center',
    width: '80%',
    marginVertical: 20,
  },
  image: {
    maxWidth: 300,
    maxHeight: 200,
    borderRadius: 9,
  },
}))
